# Prompt court pour Claude Design — NoDoomScroll HOME

Passe ce prompt + les 3 fichiers attachés à Claude Design.

---

Tu es un designer UX/UI spécialisé en apps mobiles minimalistes et premium. Je vais te donner trois fichiers de référence + un seul objectif : **générer la maquette du Home screen uniquement.**

**Les fichiers** :
1. `NoDoomScroll-Design-Brief.md` — la vision complète et le mood
2. `NoDoomScroll-Design-Tokens.md` — toutes les valeurs techniques (couleurs, typo, spacing, glassmorphism, animations)
3. `NoDoomScroll-Ecrans.md` — la spec détaillée de tous les écrans

**Objectif** : générer uniquement l'écran **HOME** (section B dans le fichier Écrans).

C'est l'écran principal. Lis la spec, utilise les tokens exactement comme indiqués, et respecte la direction du brief. L'élément signature — le streak counter en verre dépoli qui respire — doit être LE point focal. Tout le reste est calme autour.

**Livrable** : maquettes Figma (light mode + dark mode) prêtes à implémenter en Jetpack Compose Android. Indique les animations dans les commentaires Figma.

**Une fois validé, tu feras les autres écrans.**

Go.
